Usage:

~.watch_channel <CHANNEL_ID>
  Used to select which channel to watch. Must be set to something.

~.watch_message <MESSAGE_ID>
  Used to select which message to watch. Must be set to something.

~.hasi <day>
  Displays the queue for the requested day. Accepted values:
    sun
    mon
    tue
    wed
    thu
    fri
    sat
    default (no argument) - displays all days
 
 ~.update_ut
  Updates the user tables list (creates a map of Nickname keys to User IDs).
  
~.load <day> 'nick1','nick2',...
  Loads the desired day with the list of given nicknames (not case sensitive).
  
~.load_id <day> 1,2,3,...
  Loads the desired day with the list of given list of user IDs.
  
~.print_info <gid> # <day>
  Prints guild IDs, channel IDs, message IDs, and body counts for all guilds.
  If the gid flag is supplied, then only prints the above information for that specific guild.
  If a day is also supplied, then the user ID list for that day for the desired guild is printed.

~.search <NAME>
  Searches mabibase.com for <NAME> and returns the first result.
